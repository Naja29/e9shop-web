export const metadata = {
  title: 'E9Shop Admin Panel',
  description: 'Admin panel for managing E9Shop services, bookings, and users',
};

export default function AdminLayout({ children }) {
  return <>{children}</>;
}